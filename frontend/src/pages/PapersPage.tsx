import { FormEvent, useState } from 'react'
import { ExternalLink, Search } from 'lucide-react'
import { api } from '../lib/api'
import { Status } from '../components/Status'
import type { RAGResponse } from '../types'
export function PapersPage() { const [query, setQuery] = useState(''); const [data, setData] = useState<RAGResponse>(); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null); async function search(e: FormEvent) { e.preventDefault(); if (query.trim().length < 3) return setError('Enter at least 3 characters to search.'); setLoading(true); setError(null); try { setData(await api.search(query.trim())) } catch(e) { setError(e instanceof Error ? e.message : 'Search failed.') } finally { setLoading(false) } } return <div className="papers-page"><p className="eyebrow">PubMed + semantic retrieval</p><h1>Explore the evidence</h1><form className="search" onSubmit={search}><label className="sr-only" htmlFor="paper-search">Search PubMed papers</label><input id="paper-search" value={query} onChange={e => setQuery(e.target.value)} placeholder="Search a condition, treatment, or outcome"/><button><Search size={18}/> Search papers</button></form><Status loading={loading} error={error}/>{data && <><p className="result-count">{data.papers.length} ranked papers · {data.total_papers_stored} papers in evidence store</p><div className="paper-list">{data.papers.map(p => <article className="paper" key={p.pmid}><p className="match">{Math.round(p.relevance_score * 100)}% relevance</p><h2><a href={p.url || `https://pubmed.ncbi.nlm.nih.gov/${p.pmid}/`} target="_blank" rel="noreferrer">{p.title} <ExternalLink size={15}/></a></h2><p>{p.authors}</p><small>{p.journal} · {p.year} · PMID {p.pmid}</small></article>)}</div></>}</div> }
